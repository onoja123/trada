export enum TransactionStatus {
    Failed = 'Failed',
    Pending = 'Pending',
    Successful = 'Successful',
}

export enum TransactionType {
    Bill = 'Bill',
    Credit = 'Credit',
    Debit = 'Debit',
}